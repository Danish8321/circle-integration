# TODOS

Deferred items, tracked here because a doc references them but they aren't scheduled work yet.

## Deferred beyond v1 (from `docs/PRD.md` §15.4)

- **Internal maker-checker approvals** — transfers/redemptions above a threshold require a second admin approval. Deferred 2026-07-12.

## Out of scope for the reconciliation job (from `docs/DepositReconciliationPLan.md`)

- Transfer/payout reconciliation (deposit-only for now).
- Stale-`PendingCompliance` polling fallback.
- Amount/status divergence detection on already-recorded deposits.
- Notification-outbox integration for unresolvable reconciliation mismatches.
