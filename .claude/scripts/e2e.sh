#!/bin/sh
# e2e.sh [$SPEC] — Playwright against run.sh; scaffold stub until there's a flow.
#
# STUBBED: no Playwright project and no user-facing flow exist yet. Once a
# client + a real flow exist, replace this with: start run.sh in the
# background, wait for health, `npx playwright test $SPEC`, tear down.
set -eu
echo "e2e.sh: stub — no Playwright suite exists yet (no client/flow to drive)" >&2
exit 0
